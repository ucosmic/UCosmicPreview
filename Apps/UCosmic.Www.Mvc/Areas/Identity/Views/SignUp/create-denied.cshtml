@using UCosmic.Www.Mvc.Areas.Identity.Models.SignUp
@model CreateDeniedPage
@{
    Layout = MVC.Shared.Views.layout;
}
<div class="wrap-960">
    @switch (Model.Reason)
    {


        case CreateDeniedPage.DeniedBecause.ConfirmationDoesNotExist:
        case CreateDeniedPage.DeniedBecause.ConfirmationIsExpired:
            ViewBag.Title = "Your password setup window has expired";
        <p>
            For security purposes, you must set up your password within two (2) hours after
            receiving your confirmation email.
        </p>
        <p>
            Please <a href="@Url.Action(MVC.Identity.SignUp.SendEmail())">sign up again</a>,
            confirm your email address, and create your password within the 2 hour window.
        </p>
        <p>
            You will then be able to sign in.
        </p>
                       break;



        case CreateDeniedPage.DeniedBecause.UserIsSignedUp:
        case CreateDeniedPage.DeniedBecause.MemberIsSignedUp:
                       ViewBag.Title = "Your password has already been created";
        <p>
            You have already set up your password.
        </p>
        <p>
            Please <a href="@Url.Action(MVC.Identity.SignIn.SignIn())">sign in</a>, or <a href="@Url.Action(MVC.Identity.Password.ForgotPassword())">
                click here if you forgot your password</a>.
        </p>
                                                                                           break;



    }
</div>
